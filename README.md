Ansible playbook for https://mirrors.dgplug.org

```
ansible-playbook -i inventory -K mirror.yml 
```

In the above command we are running the ansible-playbook using the `inventory`.
Provide the sudo password as requried. 
